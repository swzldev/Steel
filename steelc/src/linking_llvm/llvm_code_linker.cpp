#include "llvm_code_linker.h"

code_artifact llvm_code_linker::link(const std::vector<code_artifact>& artifacts) {
	
}
bool llvm_code_linker::supports(const code_artifact& artifact) {
	return false;
}
