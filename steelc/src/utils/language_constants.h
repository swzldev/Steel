#pragma once

// this file is used to define many language constants that may
// want to be changed in the future. It does not contain constants
// that are unlikely to change such as primitive type names or
// keywords, those are defined within the lexer.

namespace language_constants {
	constexpr auto ENTRY_POINT = "Main";
}